import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oz.camTest',
  appName: 'CamTest',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
